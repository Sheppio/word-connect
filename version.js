/* Shown at the foot of the How to play sheet.

   Bump APP_VERSION by hand when releasing. APP_BUILD is rewritten with the
   deployed commit by .github/workflows/pages.yml, so it reads "dev" whenever
   the game is opened from a working copy rather than the published site. */

const APP_VERSION = '0.1.0';
const APP_BUILD = 'dev';

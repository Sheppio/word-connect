/* Shown beneath the How to play sheet.

   Bump APP_VERSION on every commit: patch for a fix or small improvement,
   minor for a new feature, major for a rewrite. APP_BUILD is rewritten with
   the deployed commit by .github/workflows/pages.yml, so it reads "dev"
   whenever the game is opened from a working copy rather than the live site. */

const APP_VERSION = '0.7.1';
const APP_BUILD = 'dev';
